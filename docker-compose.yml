version: '3.8'

services:
  web:
    container_name: aispace
    image: aispace
    build: .
    volumes:
      - .:/app
    ports:
      - "8000:8000"
    env_file: .env
    entrypoint: /app/entrypoint.sh